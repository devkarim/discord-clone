interface ChannelSidebarProps {
  serverId: string;
}

const ChannelSidebar: React.FC<ChannelSidebarProps> = ({ serverId }) => {
  return <div>Channel Sidebar</div>;
};

export default ChannelSidebar;
